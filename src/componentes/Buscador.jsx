function Buscador(change) {
  return (
    <div>
      <form onSubmit={change}>
        <input type="text" />
        <button> soy el Buscador</button>
      </form>
    </div>
  );
}

export default Buscador;
